services:
  backend:
    build:
      context: ./back-server
    ports:
      - "3008:3008"
    volumes:
      - ./back-server:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
    depends_on:
      - database
    networks:
      - cms-network

  frontend:
    build:
      context: ./front-server
    ports:
      - "8081:8081"
    volumes:
      - ./front-server:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    networks:
      - cms-network

  cmsdatabase:
    image: postgres:17
    container_name: numdiagcmsdb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: numdiagcms
    ports:
      - "5436:5432"
    volumes:
      - numdiagcms-data:/var/lib/postgresql/data
    networks:
      - cms-network

  toherodb:
    image: postgres:17
    container_name: toherodb-container
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: toheropuma
      # POSTGRES_ROLE: postgres
    ports:
      - "5437:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    user: postgres
    networks:
      - cms-network

networks:
  cms-network:
    driver: bridge

volumes:
  numdiagcms-data:
