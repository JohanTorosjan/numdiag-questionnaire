import { useState } from 'react';
import { useToast } from '../ToastSystem';

function PopUpEditAnswerSlots({ answer, answerType, onClose, onSave }) {
    const toast = useToast();
return (
    <h1>
        dkslflk
    </h1>
)
}

export default PopUpEditAnswerSlots;
